<div class="container">
	<div class="beer">
		<img src="{{this.image_url}}" alt="">

		{{#each responseFromAPI}}
			{{> beerpartial}}
			<p>{{food_pairing}}</p>
			<p>{{brewers_tips}}</p>
		{{/each}}
	</div>
</div>